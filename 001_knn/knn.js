
//Start off with what passes the first test.
class KNN {
	constructor(kSize) {
		this.kSize = kSize;
		this.points = [];
		// this.train = function () { };
	}

	train() { console.log(this); }

}

module.exports = KNN
